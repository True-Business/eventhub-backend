<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.30.xsd">

    <changeSet id="create-organization-admin-table" author="m.vozhzhov">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="organization_admin"/>
            </not>
        </preConditions>
        <comment>Created the organization_admin table.</comment>
        <createTable tableName="organization_admin">
            <column name="id" type="UUID">
                <constraints primaryKey="true"/>
            </column>
            <column name="organization_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="admin_id" type="UUID">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addUniqueConstraint tableName="organization_admin" columnNames="organization_id, admin_id" />

        <addForeignKeyConstraint baseTableName="organization_admin"
                                 baseColumnNames="organization_id"
                                 constraintName="fk_organization_admin_organizations"
                                 referencedTableName="organizations"
                                 referencedColumnNames="id"
                                 onDelete="CASCADE"/>

        <addForeignKeyConstraint baseTableName="organization_admin"
                                 baseColumnNames="admin_id"
                                 constraintName="fk_organization_admin_admins"
                                 referencedTableName="users"
                                 referencedColumnNames="id"
                                 onDelete="CASCADE"/>
    </changeSet>

</databaseChangeLog>