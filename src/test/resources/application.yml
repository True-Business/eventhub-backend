spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/testdb
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: none

  liquibase:
    change-log: classpath:db.changelog/db.changelog-master.xml
    enabled: true
