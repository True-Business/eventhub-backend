apiVersion: v1
kind: Service
metadata:
  name: {{ include "eventhub-backend.fullname" . }}-service
  labels:
    {{- include "eventhub-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eventhub-backend.selectorLabels" . | nindent 4 }}
