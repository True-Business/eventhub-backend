apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eventhub-backend.fullname" . }}-test-connection"
  labels:
    {{- include "eventhub-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eventhub-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
